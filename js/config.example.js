// Firebase 설정 예시 파일
// 이 파일을 복사하여 config.js로 만들고 실제 Firebase 설정을 입력하세요
// config.js는 .gitignore에 포함되어 있어 GitHub에 올라가지 않습니다

export const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "YOUR_MEASUREMENT_ID" // Analytics 사용 시
};

// Firebase 초기화는 main.js에서 처리
export const isFirebaseEnabled = false; // Firebase 연동 준비 완료 시 true로 변경








